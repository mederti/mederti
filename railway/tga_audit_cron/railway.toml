[build]
builder = "nixpacks"
nixpacksConfigPath = "railway/tga_audit_cron/nixpacks.toml"
rootDirectory = "/"

[deploy]
startCommand = "python railway/tga_audit_cron/run.py"
restartPolicyType = "never"
cronSchedule = "0 8 * * *"
